# =============================================================================
# HOMILIA AI BACKEND CONFIGURATION
# =============================================================================
# Copy this file to .env and update the values with your actual configuration
# 
# Required for basic functionality:
# - OPENAI_API_KEY: Your OpenAI API key for AI features
# - AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY: AWS credentials for S3 storage
# - S3_BUCKET_NAME: Name of your S3 bucket for document storage
#
# Optional (defaults work for local development):
# - OpenSearch settings (if using external OpenSearch)
# - Application settings (host, port, debug mode)
# =============================================================================

# -----------------------------------------------------------------------------
# OpenAI Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Get your API key from: https://platform.openai.com/api-keys
# The key should start with 'sk-'
OPENAI_API_KEY=your_openai_api_key_here

# -----------------------------------------------------------------------------
# AWS S3 Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Create an AWS account and S3 bucket, then generate access keys
# Make sure the user has AmazonS3FullAccess policy attached
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here
AWS_REGION=us-east-1
S3_BUCKET_NAME=your_s3_bucket_name_here

# -----------------------------------------------------------------------------
# OpenSearch Configuration (Optional - defaults work for local Docker setup)
# -----------------------------------------------------------------------------
# These settings are used when connecting to OpenSearch
# Default values work with the provided docker-compose.yml setup
OPENSEARCH_HOST=localhost
OPENSEARCH_PORT=9200
OPENSEARCH_USERNAME=admin
OPENSEARCH_PASSWORD=admin
OPENSEARCH_USE_SSL=false
OPENSEARCH_SECURITY_DISABLED=true

# -----------------------------------------------------------------------------
# Application Configuration (Optional)
# -----------------------------------------------------------------------------
# Server settings
HOST=0.0.0.0
PORT=8000

# Debug and logging
DEBUG=true
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Docker Configuration (Optional)
# -----------------------------------------------------------------------------
# Used by docker-compose.yml for OpenSearch setup
# Change this password for production use
OPENSEARCH_INITIAL_ADMIN_PASSWORD=admin

# -----------------------------------------------------------------------------
# Additional AWS Configuration (Optional)
# -----------------------------------------------------------------------------
# If you need to specify additional AWS settings
# AWS_SESSION_TOKEN=your_session_token_here  # For temporary credentials
# AWS_PROFILE=your_aws_profile_name          # For named profiles

# -----------------------------------------------------------------------------
# Development/Testing Configuration (Optional)
# -----------------------------------------------------------------------------
# Set to true to enable additional debugging features
ENABLE_DETAILED_LOGGING=false
ENABLE_REQUEST_LOGGING=true
ENABLE_CORS_DEBUG=false
